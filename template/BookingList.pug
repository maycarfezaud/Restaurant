doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  link(rel='stylesheet' href='/css/liste.css')
  title Document
header.header
  nav.nav
    .logo
      a(href='/') Quai Aquatique
    ul.nav-items.mobile-nav-links
      if userLoggedIn ==undefined
        li.nav-item.login-item
            a.login(href='/connexion') Connexion
      if userLoggedIn !=undefined
        li.nav-item.login-item
            a.login(href='/logout') Déconnexion
    .burger
      .line11
      .line22
      .line33
main
  .container
    h2 Liste des commandes
    table
      thead
        tr
          th Nom
          th email
          th heures
          th plat
      tbody#tablelist
        
footer
  p Tous droits r&eacute;serv&eacute;s &copy; Notre Restaurant 2023
script(src='/app.js')
script(src='/plat.js') 